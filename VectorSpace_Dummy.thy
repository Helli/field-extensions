theory VectorSpace_Dummy
  imports Pure
begin
(* Needed to create a dummy session without adding AFP to ROOTS *)
(* Maybe instead force the typewriter-style of "VectorSpace" manually? But how? *)
end
